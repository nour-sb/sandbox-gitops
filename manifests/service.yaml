apiVersion: v1
kind: Service
metadata:
  name: fake-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: fake-service
  ports:
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30080
